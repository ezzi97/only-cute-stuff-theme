{% comment %}
  Theme: Only Cute Stuff
  Aesthetic: Cute, Kawaii
  Currency: USD
{% endcomment %}

<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page_title }}</title>
  {{ content_for_header }}
  {% comment %} Link to main CSS file {% endcomment %}
  {{ 'theme.css' | asset_url | stylesheet_tag }}
</head>
<body>
  {% comment %} Include sections, headers, footers, etc. {% endcomment %}
  {{ content_for_layout }}

  {% comment %} Link to main JS file {% endcomment %}
  {{ 'theme.js' | asset_url | script_tag }}
</body>
</html>
